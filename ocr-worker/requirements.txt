# Dependencies for the local OCR worker (ocr_server.py / run_ocr.py).
# Runs the baidu/Unlimited-OCR model in-process on Apple Silicon (MPS) or CPU —
# no CUDA, no LM Studio/SGLang server. Pinned to the versions this worker was
# developed and verified against (Python 3.10).
torch==2.12.1
torchvision==0.27.1
transformers==4.57.1
tokenizers==0.22.2
safetensors==0.8.0
huggingface_hub==0.36.2
numpy==2.2.6
pillow==12.1.1
einops==0.8.2
# Only needed for run_ocr.py's PDF modes (imported lazily); harmless otherwise.
pymupdf==1.27.2.2
